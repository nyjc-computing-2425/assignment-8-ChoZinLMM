def reverse(text):
    """
    """
    if len(text) == 0:
      return ''
    else:
       return reverse(text[1:]) + text[0]
      
def is_palindrome(str):
    """
    """
    str = str.strip()
    if len(str) == 0:
        return ''
    else:
        
        
        
    
    